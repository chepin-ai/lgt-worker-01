name: probe-secret
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  p:
    runs-on: ubuntu-latest
    env:
      PV: ${{ secrets.TEST_PROBE_V }}
    steps:
      - run: echo "probe_len=${#PV}"
